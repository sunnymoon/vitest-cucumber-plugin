{
  "name": "@linkare/vitest-cucumberjs-monorepo",
  "version": "1.0.0",
  "author": "José Pedro Pereira <jpereira@linkare.com>",
  "license": "MIT",
  "private": "true",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build:demo": "yarn workspace @linkare/vitest-cucumberjs-demo run build",
    "build:runner": "yarn workspace @linkare/vitest-cucumberjs run build",
    "build": "yarn run build:runner && yarn run build:demo"
  },
  "devDependencies": {
    "vite": "^5.1.0",
    "vitest": "^1.3.1"
  }
}